# H-8 — 미리보기 · 변경사항 저장

<!-- CP-HOME 갱신: 2026-03-31 — O-06 정책 변경(미저장 미반영) -->

## 미리보기 (확정)

- **역할:** 방문자에게 보이는 **크리에이터 공개 홈**을 확인한다.
- **미저장 로컬 편집:** **미리보기에 반영하지 않는다**(O-06). **저장된 설정(서버에 반영된 상태)** 만 프리뷰에 보인다. 저장 전 편집을 보려면 먼저 **변경사항 저장**이 필요하다.
- **예외:** `home-H7.md`처럼 **드롭 직후 이미 서버에 반영된 순서**는 저장 버튼과 무관하게 프리뷰에 포함될 수 있다(자동 저장분은 “저장됨”으로 간주).
- 표시 수단(새 탭·모달·URL) `TBD`.

## 변경사항 저장

- **역할:** 서버에 지속 저장해야 하는 편집(커버·고정·큐레이션 구조·공개 플래그·**순서 외** 필드 등)을 반영한다.
- **순서(드래그):** `home-H7.md`에 따라 **드롭 시 이미 저장**될 수 있으므로, 본 버튼은 순서 외 변경을 묶어 저장하는 용도로 설계할 수 있다. 구현 시 중복 저장·멱등 처리 `TBD`.
- 클릭 시: 로딩 → 성공/실패 피드백 `TBD`.

## 저장 전 검증 (초안)

| 검증 | 내용 |
|------|------|
| 필수 | 커버 미설정 시 저장 허용 여부 `TBD` |
| 큐레이션 | 빈 그룹 허용 여부 `TBD` |
| 동시 편집 | 충돌 처리 `TBD` |

## 체크포인트

- **`CP-HOME-DONE`:** O-01~O-06 반영 완료.

**다음:** `post-PM1.md`
